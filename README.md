# cloudsmith-cli startup performance evidence

Flame graphs for the performance PR stack #373-#377. Each image is an
icicle chart generated from `python -X importtime` output: parents sit
above children and width equals cumulative import time.

This branch holds PR evidence only. Delete it after the stack merges.
